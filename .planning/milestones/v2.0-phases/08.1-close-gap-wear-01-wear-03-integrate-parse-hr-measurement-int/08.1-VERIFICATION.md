---
phase: 08.1-close-gap-wear-01-wear-03-integrate-parse-hr-measurement-int
verified: 2026-06-04T00:00:00Z
status: passed
score: 8/8 must-haves verified
overrides_applied: 0
---

# Phase 08.1: WEAR-01 / WEAR-03 Gap Closure + CR-02 Verification Report

**Phase Goal:** Integrate parse_hr_measurement into upload.get_recent_decoded_streams so HR monitor upload payloads contain decoded hr/rr stream data. Also fix CR-02 (device_id filter). No iOS changes, no UI.
**Verified:** 2026-06-04
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | An HR monitor 0x2A37 GATT notification imported via capture.import_frame_batch produces a decoded_frames row with device_type == HR_MONITOR | VERIFIED | `capture_import.rs` lines 637-683: `DeviceType::HrMonitor` branch bypasses `parse_frame`, constructs a pseudo-ParsedFrame with `payload_hex = hex::encode(&raw_bytes)`, inserts via `store.insert_decoded_frame` |
| 2 | upload.get_recent_decoded_streams returns a non-empty hr stream for stored HR monitor frames, each entry carrying bpm and rr_intervals | VERIFIED | `bridge.rs` lines 3155-3179: `if frame.device_type == "HR_MONITOR"` branch decodes payload_hex, calls `parse_hr_measurement`, pushes `{"ts": ts_opt, "bpm": measurement.hr_bpm, "rr_intervals": measurement.rr_intervals_ms}` to hr Vec; integration test `bridge_hr_monitor_upload_stream_contains_bpm_and_rr` passes (bpm=72, rr≈1000ms) |
| 3 | RR intervals from HR monitor frames are embedded inside the hr[] entries, not pushed to the top-level rr stream | VERIFIED | `bridge.rs` lines 3158, 3177: D-02 comment explicit; hr push includes `rr_intervals` field; no rr.push() in HR monitor branch; test asserts `result["rr"].is_empty()` |
| 4 | When device_id is set, only frames belonging to that device (by stored device_model) are returned | VERIFIED | `bridge.rs` lines 3064-3080: `store.raw_evidence(&frame.evidence_id)` lookup, `evidence.device_model != args.device_id` check with `continue` on mismatch; `bridge_hr_monitor_upload_stream_device_id_filter` test passes (imports 2 devices, filters to 1) |
| 5 | A stored HR monitor frame whose bytes fail parse_hr_measurement is skipped without crashing | VERIFIED | `bridge.rs` lines 3162-3169: `hex::decode` Err → `continue`; `parse_hr_measurement` Err → `continue`; no unwrap or panic |
| 6 | No chrono dependency added | VERIFIED | `grep -c chrono Rust/core/Cargo.toml` returns 0; `unix_from_iso8601` helper at `bridge.rs` lines 3250-3298 implements ISO-8601 parsing via str-slicing and Julian-day arithmetic |
| 7 | The stale "no per-row filtering is needed" comment is removed and a device_model-based filter is present | VERIFIED | `grep "no per-row filtering" bridge.rs` returns no matches; CR-02 device_model filter with `continue` is present at lines 3064-3080 |
| 8 | cargo test suite passes with 3 new integration tests and no regressions | VERIFIED | `cargo test --test bridge_tests bridge_hr_monitor` → 3 passed, 0 failed; full `cargo test` → all test files green, 0 failures |

**Score:** 8/8 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `Rust/core/src/capture_import.rs` | DeviceType::HrMonitor branch storing raw GATT bytes as decoded_frames row | VERIFIED | Lines 637-683: branch exists, both CRC flags true, `hex::encode` on raw_bytes for payload_hex |
| `Rust/core/src/bridge.rs` | HR monitor branch in upload_get_recent_decoded_streams_bridge calling parse_hr_measurement + real device_id filter | VERIFIED | Lines 3058-3080 (device_id filter), 3155-3179 (HR monitor branch), 3250-3298 (unix_from_iso8601 helper) |
| `Rust/core/tests/bridge_tests.rs` | 3 integration tests covering HR monitor upload extraction, no-RR case, and device_id filtering | VERIFIED | Functions at lines 8838, 8903, 8949; all 3 pass |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `bridge.rs upload_get_recent_decoded_streams_bridge` | `crate::heart_rate_gatt_protocol::parse_hr_measurement` | Direct function call on hex-decoded payload_hex at line 3167 | WIRED | `grep "parse_hr_measurement" bridge.rs` → line 3167 inside the upload function |
| `capture_import.rs import_captured_frame_timed` | `store.insert_decoded_frame` | HrMonitor branch before parse_frame, using pseudo-ParsedFrame | WIRED | Lines 637-673: `DeviceType::HrMonitor` branch calls `store.insert_decoded_frame` with constructed frame |
| `bridge_tests.rs HR monitor test` | `capture.import_frame_batch + upload.get_recent_decoded_streams` | `request()` JSON-RPC helper against tempdir SQLite | WIRED | All 3 tests exercise the full end-to-end path and pass |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|--------------------|--------|
| `upload_get_recent_decoded_streams_bridge` (bridge.rs) | `hr: Vec<serde_json::Value>` | `store.decoded_frames_between` → HrMonitor branch → `parse_hr_measurement` | Yes — bytes from SQLite decoded_frames.payload_hex, parsed via `parse_hr_measurement` returning real bpm and rr_intervals | FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| 3 HR monitor integration tests pass | `cargo test --test bridge_tests bridge_hr_monitor` | 3 passed; 0 failed | PASS |
| Full test suite green | `cargo test` (all test files) | 0 failures across all test files | PASS |
| Build clean | `cargo build` | Completes with no errors or warnings | PASS |

---

### Requirements Coverage

| Requirement | Phase | Description | Status | Evidence |
|-------------|-------|-------------|--------|----------|
| WEAR-01 | 08.1 | parse_hr_measurement integrated into upload path | SATISFIED | `bridge.rs` line 3167 calls `parse_hr_measurement`; `bridge_hr_monitor_upload_stream_contains_bpm_and_rr` proves end-to-end bpm+RR extraction |
| WEAR-03 | 08.1 | Upload payload correctly identifies HR monitor data with decoded hr stream | SATISFIED | HR monitor branch produces `{"ts": f64, "bpm": u16, "rr_intervals": [f64]}` entries in the `hr` stream; separate from WHOOP data by `device_type == "HR_MONITOR"` guard |
| CR-02 | 08.1 | device_id filter implemented | SATISFIED | `store.raw_evidence(evidence_id).device_model` comparison at lines 3064-3080; `bridge_hr_monitor_upload_stream_device_id_filter` test proves filtering |

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| bridge.rs | 3050 | `let rr` (not `let mut rr`) | Info | SUMMARY claimed `mut` was kept for D-02 consistency but the code uses `let rr`. Functionally correct: `rr` is never mutated in the HR monitor path (RR is embedded in hr entries per D-02). No behavioral impact. |

No TODO, FIXME, TBD, or XXX markers found in any of the three modified files.

---

### Human Verification Required

None. All behavioral checks are exercised by deterministic integration tests that were run and passed.

---

### Gaps Summary

No gaps. All 8 must-haves are verified in the codebase.

One cosmetic discrepancy: the SUMMARY.md stated `let mut rr` was kept for consistency, but `bridge.rs` line 3050 uses `let rr` (immutable). This is not a gap — it is a SUMMARY inaccuracy. The immutable form is correct because `rr` is never written to (all HR monitor RR data goes into `hr` entries per D-02), and the compiler emitted no warning about it.

---

_Verified: 2026-06-04_
_Verifier: Claude (gsd-verifier)_
