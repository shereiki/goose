---
phase: 18-coach-multi-provider
plan: 06
type: execute
wave: 6
depends_on: ["18-01", "18-02", "18-03", "18-04", "18-05"]
files_modified:
  - GooseSwiftTests/CoachProviderTests.swift
autonomous: false
requirements: [COACH-01, COACH-05, COACH-06]
user_setup: []

must_haves:
  truths:
    - "The full app target builds with zero errors and the full GooseSwiftTests suite is green"
    - "All four providers (per D-01) conform to CoachProvider and the registry exposes them"
    - "An existing ChatGPT user upgrading to this build still has a working Coach with no re-authentication"
    - "Each configured provider (ChatGPT, Claude, Custom) streams a response end-to-end"
  artifacts:
    - path: "GooseSwiftTests/CoachProviderTests.swift"
      provides: "Finalised COACH-01 protocol conformance assertions across all four providers"
      contains: "func test"
  key_links:
    - from: "GooseSwiftTests"
      to: "all four CoachProvider conformances"
      via: "xcodebuild test full suite"
      pattern: "CoachProvider"
---

<objective>
Integration and verification wave: prove the whole feature hangs together. Run the full build + test suite, finalise the COACH-01 protocol-conformance assertions to cover all four providers, and perform the manual migration smoke test (COACH-06) plus end-to-end streaming verification for each configured provider.

Purpose: Catch cross-wave integration regressions and confirm the phase success criteria before `/gsd-verify-work`.
Output: a clean full-suite test run and a documented manual verification record.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-coach-multi-provider/18-CONTEXT.md
@.planning/phases/18-coach-multi-provider/18-VALIDATION.md
@.planning/phases/18-coach-multi-provider/18-01-SUMMARY.md
@.planning/phases/18-coach-multi-provider/18-02-SUMMARY.md
@.planning/phases/18-coach-multi-provider/18-03-SUMMARY.md
@.planning/phases/18-coach-multi-provider/18-04-SUMMARY.md
@.planning/phases/18-coach-multi-provider/18-05-SUMMARY.md

@GooseSwift/CoachProviderProtocol.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalise COACH-01 protocol-conformance test + full suite green</name>
  <files>GooseSwiftTests/CoachProviderTests.swift</files>
  <read_first>
    GooseSwiftTests/CoachProviderTests.swift (Wave 0/Wave 1 stub)
    GooseSwift/CoachProviderProtocol.swift (CoachProvider protocol + registry)
    .planning/phases/18-coach-multi-provider/18-VALIDATION.md (Per-Task Verification Map)
  </read_first>
  <action>
    In GooseSwiftTests/CoachProviderTests.swift, finalise `testCoachProviderProtocolHasRequiredMembers()` to iterate `CoachProviderRegistry().allProviders` and assert for each: non-empty `id`, non-empty `displayName`, and that `availablePresets` is accessible (ChatGPT/Claude/Gemini return non-empty; Custom may be empty until configured — assert accessible, not non-empty for custom). This proves all four conformances compile and expose the D-06 members (COACH-01). Replace the network-skipped `testSendReturnsAsyncStreamShape()` with a compile-time assertion that the closure type of `send` matches `(…) async throws -> AsyncStream<String>` — e.g. assign a provider's `send` to a typed local `let _: ([CoachChatMessage], String, CoachModelPreset) async throws -> AsyncStream<String> = provider.send` to prove the signature without making a network call.

    Then run the FULL test suite to catch cross-wave integration regressions.
  </action>
  <verify>
    <automated>xcodebuild test -scheme GooseSwift -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AsyncStream<String>" GooseSwiftTests/CoachProviderTests.swift` returns at least 1
    - `grep -c "XCTSkip" GooseSwiftTests/CoachProviderTests.swift` returns 0
    - Full `xcodebuild test` run reports `** TEST SUCCEEDED **` (no failing tests)
  </acceptance_criteria>
  <done>COACH-01 conformance asserted for all four providers; full GooseSwiftTests suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Migration smoke test + end-to-end streaming for each provider</name>
  <files>GooseSwift/CoachProviderProtocol.swift</files>
  <action>Pause for the user to run the migration smoke test and per-provider streaming verification. Present the what-built and how-to-verify content, then wait for the resume-signal.</action>
  <verify><human-check>User confirms ChatGPT migration (no re-auth) and per-provider streaming work</human-check></verify>
  <done>User types "approved" after confirming COACH-06 migration and live streaming for each configured provider.</done>
  <what-built>The complete Coach multi-provider feature: CoachProvider protocol + registry, four providers (ChatGPT/Claude/Gemini/Custom), and the settings sheet. This checkpoint verifies the two behaviours that cannot be automated (per VALIDATION.md Manual-Only Verifications): COACH-06 migration of the existing ChatGPT key, and live streaming per provider.</what-built>
  <how-to-verify>
    1. MIGRATION (COACH-06): On a device/simulator that already has an existing ChatGPT OAuth token in Keychain (from before this build), cold-launch the app. Open the Coach tab. Confirm: no re-authentication is required, ChatGPT is the active provider, and existing Coach conversation history is intact. Send a message → reply streams.
    2. CLAUDE (COACH-03): Open settings → select Claude → enter an Anthropic API key → Save. Select a Claude preset, send a message → confirm tokens stream in real time.
    3. CUSTOM (COACH-04): Open settings → select Custom → enter an OpenAI-compatible base URL (https), API key, and model ID → Save Endpoint. Send a message → confirm tokens stream.
    4. GEMINI (COACH-03): If a Google OAuth Client ID is available, confirm the Wave 4 Gemini flow still works after full integration (sign in, send, stream). Otherwise record as "deferred — no Client ID".
    5. REGRESSION: Confirm switching the active provider mid-session and starting a new conversation behaves correctly, and that no emoji / English leakage appears in pt-PT mode.
  </how-to-verify>
  <resume-signal>Type "approved" if migration + per-provider streaming work, or describe failures per provider.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → all four provider backends | Credentials + chat content cross to ChatGPT/Anthropic/Google/custom over TLS |
| app → Keychain | All provider credentials read during integration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-17 | Information Disclosure | Cross-provider credential leakage | mitigate | Each provider uses a distinct Keychain service namespace (verified by per-wave threat models T-18-04/08/11); integration test confirms no shared key path |
| T-18-18 | Tampering | Active provider switch loads wrong credentials | mitigate | Registry resolves credentials per provider id; manual regression (Task 2 step 5) confirms switching providers uses the correct backend |
| T-18-SC | Tampering | npm/pip/cargo installs | mitigate | Zero external packages across the whole phase (D-08); confirmed at integration |
</threat_model>

<verification>
- `xcodebuild test -scheme GooseSwift` full suite → `** TEST SUCCEEDED **`
- `xcodebuild build -scheme GooseSwift` → zero errors
- Manual checkpoint (Task 2): migration smoke test + per-provider streaming
</verification>

<success_criteria>
1. Full app builds and full GooseSwiftTests suite is green (COACH-01)
2. All four providers conform to CoachProvider and are exposed by the registry
3. Existing ChatGPT user migrates with no re-auth and intact history (COACH-06)
4. Claude and Custom (and Gemini if Client ID available) stream end-to-end (COACH-05)
</success_criteria>

<artifacts>
## Artifacts this phase produces (Wave 6)

| Symbol | File | Kind |
|--------|------|------|
| Finalised `CoachProviderTests` | GooseSwiftTests/CoachProviderTests.swift | COACH-01 conformance suite |
| (verification only) full-suite green + manual record | — | integration gate |
</artifacts>

<output>
Create `.planning/phases/18-coach-multi-provider/18-06-SUMMARY.md` when done
</output>
