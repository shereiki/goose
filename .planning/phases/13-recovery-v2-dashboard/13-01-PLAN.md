---
phase: 13
plan: 01
type: auto
autonomous: true
---

# Phase 13 Plan 01: Recovery V2 Dashboard Bridge Trigger

## Objective

Wire `RecoveryV2OverviewPage` to call `runPacketScores()` on appear and on new packet imports, so the bridge-backed recovery score, HRV, RHR and trend rows are populated from real captured data instead of defaulting to zero.

## Context

- `RecoveryV2OverviewPage` was complete but never triggered `runPacketScores()`
- `packetScoreReports["recovery"]` remained empty → `recoveryScore` always 0
- Pattern established in `SleepV2OverviewPage` and `HealthDashboardViews`

## Tasks

### Task 1: Add onAppear + onChange trigger and ProgressView guard (type: auto)

**Files:** `GooseSwift/HealthRecoveryStressViews.swift`

**Changes:**
1. Wrap `SleepV2Hero(...)` in `if store.packetScoreStatus.hasPrefix("Extracting")` / `else` block showing a `ProgressView` during extraction
2. Add `.onAppear { store.loadBridgeCatalogsIfNeeded(); store.runPacketScores() }` after final `.sheet` modifier
3. Add `.onChange(of: model.packetImportRevision) { _, _ in store.runPacketScores() }` to refresh on packet imports

## Verification

- `grep -c "runPacketScores\|loadBridgeCatalogsIfNeeded"` → ≥2
- `grep -c "packetImportRevision"` → 1
- `grep -c "Extracting"` → 1
- `cargo test -p goose-core` → passes
