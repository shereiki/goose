---
phase: 08.1-close-gap-wear-01-wear-03-integrate-parse-hr-measurement-int
plan: 02
type: tdd
wave: 2
depends_on:
  - 08.1-01
files_modified:
  - Rust/core/tests/bridge_tests.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "An integration test proves upload.get_recent_decoded_streams returns a non-empty hr stream with bpm and rr_intervals for an HR monitor frame imported via capture.import_frame_batch"
    - "An integration test proves the device_id filter excludes frames belonging to a different device"
    - "cargo test passes with the new tests"
  artifacts:
    - path: "Rust/core/tests/bridge_tests.rs"
      provides: "Two new integration tests covering HR monitor upload extraction and device_id filtering"
      contains: "upload.get_recent_decoded_streams"
  key_links:
    - from: "bridge_tests.rs new HR monitor test"
      to: "capture.import_frame_batch + upload.get_recent_decoded_streams bridge methods"
      via: "request() JSON-RPC helper against a tempdir SQLite"
      pattern: "upload.get_recent_decoded_streams"
---

<objective>
Prove the HR monitor upload pipeline end-to-end with at least two integration tests in bridge_tests.rs. These tests are the behavioral verification for plan 08.1-01 (which makes only build-level guarantees on its own).

Test 1 (WEAR-01/WEAR-03 proof): import an HR monitor 0x2A37 frame via capture.import_frame_batch, then call upload.get_recent_decoded_streams and assert the hr stream is non-empty and the entry carries bpm + rr_intervals.

Test 2 (CR-02 proof): import frames from two distinct devices (distinct device_model), call upload.get_recent_decoded_streams with a device_id matching only one, and assert the other device's frames are excluded.

Purpose: lock in the gap closure with regression-proof tests; the original Phase 8 work shipped without these and the gap went undetected.
Output: two passing integration tests; full cargo test suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.1-close-gap-wear-01-wear-03-integrate-parse-hr-measurement-int/08.1-CONTEXT.md
@.planning/phases/08.1-close-gap-wear-01-wear-03-integrate-parse-hr-measurement-int/08.1-01-PLAN.md

@Rust/core/tests/bridge_tests.rs
@Rust/core/tests/heart_rate_gatt_protocol_tests.rs
@Rust/core/src/bridge.rs
</context>

<test_fixtures>
Use a known-good 0x2A37 byte vector taken from heart_rate_gatt_protocol_tests.rs (test_parses_hr_with_rr_intervals):

- Bytes: flags=0x10 (8-bit HR, RR present), hr=60 (0x3C), RR raw = 0x0400 LE = 1024 → 1000.0 ms.
- Full GATT notification bytes as hex (this is the `frame_hex` to send to capture.import_frame_batch AND what the bridge will hex::decode then feed to parse_hr_measurement): "103c0004"
- Expected after parse: hr_bpm == 60, rr_intervals_ms == [≈1000.0] (one interval; assert with tolerance < 0.5).

A no-RR alternative (flags=0x00, hr=72): "0048" → hr_bpm == 72, rr_intervals == [].
</test_fixtures>

<tasks>

<task type="tdd">
  <name>Task 1: Integration test — HR monitor frame produces hr stream with bpm + rr_intervals</name>
  <read_first>
    - Rust/core/tests/bridge_tests.rs (lines 1-52 for imports and the `request()` helper usage; bridge_imports_app_captured_frame_batch_into_sqlite_and_timeline lines 1605-1700 for the capture.import_frame_batch request shape; bridge_gen4_upload_device_generation_field_is_set_correctly line ~465 for the GEN4 import-then-assert pattern)
    - Rust/core/tests/heart_rate_gatt_protocol_tests.rs (test_parses_hr_with_rr_intervals lines 29-39 — source of the byte vector)
    - Rust/core/src/bridge.rs (UploadGetRecentDecodedStreamsArgs lines 3019-3024 — args are database_path, device_id, since_ts; the result shape lines 3146-3156 — keys hr/rr/events/.../frame_count)
  </read_first>
  <behavior>
    - RED: write the test asserting a non-empty hr stream; before plan 08.1-01 it would fail (empty hr) — but 08.1-01 is a dependency, so on a correct codebase the test passes (GREEN). If the executor runs this before 08.1-01 is merged, the test MUST fail with an empty hr stream, confirming the test actually exercises the new path.
    - The hr stream has exactly 1 entry for a single imported HR monitor frame.
    - hr[0]["bpm"] == 60.
    - hr[0]["rr_intervals"] is an array of length 1 with value ≈ 1000.0 (tolerance < 0.5).
    - The top-level rr stream is empty (D-02: HR monitor RR is embedded in hr, not pushed to rr).
    - hr[0]["ts"] is a number (captured_at parsed to f64), not null, for a well-formed captured_at like "2026-05-28T12:00:00.000Z".
  </behavior>
  <action>
    Add a #[test] fn bridge_hr_monitor_upload_streams_contain_bpm_and_rr_intervals(). Create a tempdir SQLite path (tempfile::tempdir pattern used throughout the file). Send a capture.import_frame_batch request via the existing `request(serde_json::json!({...}))` helper with one frame: evidence_id "hr-monitor-frame-1", source "ios.corebluetooth.notification", captured_at "2026-05-28T12:00:00.000Z", device_model "Polar H10 1234", frame_hex "103c0004", sensitivity "user-owned-capture", device_type "HR_MONITOR". Use parser_version "goose-core/bridge-test". Assert the import response.ok is true. (The import result for an HR monitor frame should report imported_frame true — assert on `result["results"][0]["imported_frame"] == true` if include_results is set; include "include_results": true in args to enable that assertion.) Then send an upload.get_recent_decoded_streams request with args database_path, device_id "" (empty — no filter), since_ts 0.0 (epoch, captures everything up to now). Assert response.ok. Bind result = response.result.unwrap(). Assert result["hr"].as_array().unwrap().len() == 1. Assert result["hr"][0]["bpm"] == 60. Assert result["hr"][0]["rr_intervals"].as_array().unwrap().len() == 1 and ((result["hr"][0]["rr_intervals"][0].as_f64().unwrap() - 1000.0).abs() < 0.5). Assert result["rr"].as_array().unwrap().is_empty(). Assert result["hr"][0]["ts"].as_f64().is_some(). Match the json!/request idiom and assertion style already used in bridge_imports_app_captured_frame_batch_into_sqlite_and_timeline.
  </action>
  <verify>
    <automated>cd Rust/core && cargo test --test bridge_tests bridge_hr_monitor_upload_streams_contain_bpm_and_rr_intervals -- --nocapture 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Test-command: `cd Rust/core && cargo test --test bridge_tests bridge_hr_monitor_upload_streams_contain_bpm_and_rr_intervals` passes
    - Source assertion: the test asserts `hr[0]["bpm"]` == 60 and rr_intervals length == 1 with ≈1000.0
    - Source assertion: the test asserts the top-level `rr` array is empty
    - Source assertion: the test sends frame_hex "103c0004" with device_type "HR_MONITOR" via capture.import_frame_batch
  </acceptance_criteria>
  <done>The test imports an HR monitor frame and proves upload.get_recent_decoded_streams returns one hr entry with bpm 60 and one rr_interval ≈1000.0ms, with the top-level rr stream empty. The test passes.</done>
</task>

<task type="tdd">
  <name>Task 2: Integration test — device_id filter excludes other devices</name>
  <read_first>
    - Rust/core/tests/bridge_tests.rs (the test added in Task 1, for the import + upload request idiom)
    - Rust/core/src/bridge.rs (the device_id filter logic added in 08.1-01 Task 2 — filters by raw_evidence device_model; UploadGetRecentDecodedStreamsArgs.device_id)
    - .planning/phases/08.1-close-gap-wear-01-wear-03-integrate-parse-hr-measurement-int/08.1-01-PLAN.md (plan_deviations item 3 — device_id maps to device_model)
  </read_first>
  <behavior>
    - Two HR monitor frames are imported with different device_model values ("Polar H10 AAAA" and "Garmin HRM BBBB"), each in its own raw_evidence row (distinct evidence_id).
    - Calling upload.get_recent_decoded_streams with device_id == "Polar H10 AAAA" returns hr entries ONLY from the Polar frame (length 1).
    - Calling with device_id == "" (empty) returns hr entries from BOTH frames (length 2) — empty device_id disables filtering.
  </behavior>
  <action>
    Add a #[test] fn bridge_hr_monitor_upload_device_id_filter_excludes_other_devices(). Create a tempdir SQLite. Send one capture.import_frame_batch request containing TWO frames in the frames array: frame A evidence_id "hr-dev-a", device_model "Polar H10 AAAA", captured_at "2026-05-28T12:00:00.000Z", frame_hex "0048" (flags 0x00, hr 72), device_type "HR_MONITOR"; frame B evidence_id "hr-dev-b", device_model "Garmin HRM BBBB", captured_at "2026-05-28T12:00:01.000Z", frame_hex "0050" (flags 0x00, hr 80), device_type "HR_MONITOR". Both source "ios.corebluetooth.notification", sensitivity "user-owned-capture". Assert import ok. Then send upload.get_recent_decoded_streams with device_id "Polar H10 AAAA", since_ts 0.0; assert response.ok and result["hr"].as_array().unwrap().len() == 1 and result["hr"][0]["bpm"] == 72. Then send a second upload.get_recent_decoded_streams with device_id "" (empty), since_ts 0.0; assert result["hr"].as_array().unwrap().len() == 2. Use the same request()/json! idiom as Task 1.
  </action>
  <verify>
    <automated>cd Rust/core && cargo test --test bridge_tests bridge_hr_monitor_upload_device_id_filter_excludes_other_devices -- --nocapture 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Test-command: `cd Rust/core && cargo test --test bridge_tests bridge_hr_monitor_upload_device_id_filter_excludes_other_devices` passes
    - Source assertion: the test imports two frames with distinct device_model values and asserts a filtered query returns length 1 with bpm 72
    - Source assertion: the test asserts an empty device_id returns length 2 (both devices)
  </acceptance_criteria>
  <done>The test proves device_id filtering by device_model: a specific device_id returns only that device's hr entries; an empty device_id returns all. The test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> bridge | tests feed crafted byte vectors through the public bridge JSON-RPC surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1-T01 | Tampering | test fixtures | accept | Fixtures are deterministic known-good vectors copied from heart_rate_gatt_protocol_tests.rs; no external input. |
| T-08.1-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies; tempfile already a dev-dependency. No install tasks. |
</threat_model>

<verification>
- `cd Rust/core && cargo test --test bridge_tests` — both new tests pass and no existing test regresses.
- `cd Rust/core && cargo test` — full suite green.
</verification>

<success_criteria>
- bridge_hr_monitor_upload_streams_contain_bpm_and_rr_intervals passes, proving WEAR-01/WEAR-03 closure.
- bridge_hr_monitor_upload_device_id_filter_excludes_other_devices passes, proving CR-02 closure.
- Full cargo test suite remains green.
</success_criteria>

<output>
Create `.planning/phases/08.1-close-gap-wear-01-wear-03-integrate-parse-hr-measurement-int/08.1-02-SUMMARY.md` when done.
</output>
