---
phase: 18-coach-multi-provider
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - GooseSwift/CoachProviderProtocol.swift
  - GooseSwift/CoachChatModel.swift
  - GooseSwift/OpenAICoachChat.swift
  - GooseSwift/ChatGPTCoachProvider.swift
  - GooseSwift/CoachChatTypes.swift
  - GooseSwift/CoachView.swift
  - GooseSwift/CoachChatScreen.swift
  - GooseSwiftTests/CoachProviderTests.swift
  - GooseSwiftTests/CoachProviderRegistryTests.swift
  - GooseSwiftTests/CoachKeychainTests.swift
  - GooseSwiftTests/ClaudeProviderTests.swift
  - GooseSwiftTests/CustomEndpointProviderTests.swift
autonomous: true
requirements: [COACH-01, COACH-06]
user_setup: []

must_haves:
  truths:
    - "CoachProvider protocol exists and compiles with at least the ChatGPT conformance"
    - "CoachProviderRegistry is @Observable, exposes activeProvider + allProviders, persists active provider ID to UserDefaults"
    - "CoachChatModel (renamed from OpenAICoachChatModel) is @Observable @MainActor and routes send() through the registry's active provider"
    - "On first launch with an existing ChatGPT Keychain token, the registry sets ChatGPTCoachProvider as the active provider with no user action"
    - "Coach tab still streams ChatGPT responses with no behaviour regression"
  artifacts:
    - path: "GooseSwift/CoachProviderProtocol.swift"
      provides: "CoachProvider protocol + CoachProviderRegistry @Observable class"
      contains: "protocol CoachProvider"
    - path: "GooseSwift/CoachChatModel.swift"
      provides: "@Observable CoachChatModel coordinator delegating to registry"
      contains: "CoachChatModel"
    - path: "GooseSwift/ChatGPTCoachProvider.swift"
      provides: "ChatGPTCoachProvider wrapping existing CodexSelfContainedAuthClient + OpenAIResponsesClient"
      contains: "ChatGPTCoachProvider"
    - path: "GooseSwiftTests/CoachProviderTests.swift"
      provides: "Wave 0 stub covering COACH-01 protocol shape + AsyncStream"
      contains: "func test"
  key_links:
    - from: "GooseSwift/CoachChatModel.swift"
      to: "CoachProviderRegistry.activeProvider"
      via: "send() reads registry.activeProvider and calls provider.send(messages:systemPrompt:preset:)"
      pattern: "registry\\.activeProvider"
    - from: "GooseSwift/CoachView.swift"
      to: "CoachChatModel"
      via: "@State private var chat = CoachChatModel()"
      pattern: "@State private var chat = CoachChatModel"
---

<objective>
Establish the provider abstraction that the rest of Phase 18 builds on: the `CoachProvider` protocol (D-06), the `CoachProviderRegistry` @Observable class (D-07), the refactor of `OpenAICoachChatModel` → `CoachChatModel` (@Observable @MainActor, D-07), and the `ChatGPTCoachProvider` that wraps the existing ChatGPT logic behind the protocol. Also create all Wave 0 test stubs the later waves depend on.

This is the interface-first wave: it defines the contracts (protocol + registry) before any new provider is implemented, so Waves 2-4 implement against a stable interface.

Purpose: Decouple the Coach UI from the hardcoded ChatGPT backend so additional providers can be added without touching the chat model or views. COACH-06 (existing ChatGPT key works untouched) is satisfied here because `ChatGPTCoachProvider` wraps the existing `CodexSelfContainedAuthClient` — the registry picks up the existing Keychain token automatically (no data migration).
Output: `CoachProviderProtocol.swift`, `CoachChatModel.swift`, `ChatGPTCoachProvider.swift`, extended `CoachModelPreset`, rewired call sites, and five Wave 0 test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-coach-multi-provider/18-CONTEXT.md
@.planning/phases/18-coach-multi-provider/18-RESEARCH.md
@.planning/phases/18-coach-multi-provider/18-PATTERNS.md
@.planning/phases/18-coach-multi-provider/18-VALIDATION.md

@GooseSwift/OpenAICoachChat.swift
@GooseSwift/CoachChatTypes.swift
@GooseSwift/OpenAICoachResponsesClient.swift
@GooseSwift/CoachView.swift
@GooseSwift/CoachChatScreen.swift
@GooseSwift/GooseAppModel.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 test stubs for all five Phase 18 test files</name>
  <files>GooseSwiftTests/CoachProviderTests.swift, GooseSwiftTests/CoachProviderRegistryTests.swift, GooseSwiftTests/CoachKeychainTests.swift, GooseSwiftTests/ClaudeProviderTests.swift, GooseSwiftTests/CustomEndpointProviderTests.swift</files>
  <read_first>
    GooseSwiftTests/HRMonitorStateTests.swift (existing test style: `import XCTest` + `@testable import GooseSwift` + `final class … : XCTestCase`)
    .planning/phases/18-coach-multi-provider/18-VALIDATION.md (Per-Task Verification Map + Wave 0 Requirements)
  </read_first>
  <action>
    Create all five XCTest files in GooseSwiftTests/, each beginning with `import XCTest` and `@testable import GooseSwift`, each declaring a `final class …Tests: XCTestCase`. The test target globs the directory, so new files compile automatically — verify they appear in the test run.

    CoachProviderTests.swift — covers COACH-01. Add `testCoachProviderProtocolHasRequiredMembers()` that constructs a `ChatGPTCoachProvider` and asserts `provider.id`, `provider.displayName`, `provider.availablePresets` are accessible/non-empty (compile-time proof the D-06 protocol shape exists). Add `testSendReturnsAsyncStreamShape()` marked with `throw XCTSkip("Wave 1: requires network; AsyncStream shape proven by compile")` so the file compiles and documents intent.

    CoachProviderRegistryTests.swift — covers COACH-06. Add `testRegistryPersistsActiveProviderID()` that constructs `CoachProviderRegistry()`, calls `selectProvider(id:)` for the ChatGPT provider, and asserts `UserDefaults.standard.string(forKey: "goose.coach.activeProviderId")` equals "chatgpt". Add `testRegistryExposesAllFourProviders()` asserting `CoachProviderRegistry().allProviders.count == 4` and provider ids include "chatgpt", "claude", "gemini", "custom" — but mark it `throw XCTSkip("providers added in Waves 2-4; re-enabled in Plan 18-05")` because only ChatGPT exists this wave.

    CoachKeychainTests.swift — covers COACH-02. Add `testClaudeKeychainRoundtrip()` and `testCustomEndpointKeychainRoundtrip()` both marked `throw XCTSkip("Wave 2/3: Keychain helper not yet implemented")`.

    ClaudeProviderTests.swift — covers COACH-03. Add `testClaudeDeltaExtraction()` marked `throw XCTSkip("Wave 2: ClaudeCoachProvider not yet implemented")`.

    CustomEndpointProviderTests.swift — covers COACH-04. Add `testCustomEndpointURLValidationRejectsHTTPExternal()` marked `throw XCTSkip("Wave 3: CustomEndpointCoachProvider not yet implemented")`.

    Per the Nyquist rule, stubs that reference not-yet-existing symbols must use XCTSkip and NOT reference those symbols (to keep the file compilable this wave). Only CoachProviderTests and the persistence test in CoachProviderRegistryTests reference symbols created in this wave; the other tests are skip-only placeholders.
  </action>
  <verify>
    <automated>xcodebuild build-for-testing -scheme GooseSwift -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | grep -v '^#' | grep -c 'error:' | { read n; [ "$n" -eq 0 ] && echo "BUILD OK" || echo "BUILD ERRORS: $n"; }</automated>
  </verify>
  <acceptance_criteria>
    - All five files exist under GooseSwiftTests/ and each starts with `import XCTest` and `@testable import GooseSwift`
    - `build-for-testing` reports zero `error:` lines
    - CoachProviderRegistryTests references `CoachProviderRegistry` and the `goose.coach.activeProviderId` UserDefaults key
    - The Wave 2/3/4 stubs use `XCTSkip` and reference no symbols created in later waves
  </acceptance_criteria>
  <done>Five test files compile under the GooseSwiftTests target; build-for-testing succeeds with zero errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CoachProvider protocol + CoachProviderRegistry + extended CoachModelPreset</name>
  <files>GooseSwift/CoachProviderProtocol.swift, GooseSwift/CoachChatTypes.swift</files>
  <behavior>
    - Setting the registry's active provider via selectProvider(id:) persists its id to UserDefaults key "goose.coach.activeProviderId"
    - On init, if "goose.coach.activeProviderId" is unset and ChatGPT is authenticated, activeProvider resolves to the ChatGPT provider
    - CoachModelPreset gains cases claudeOpus48/claudeSonnet46/claudeHaiku45/gemini25Pro/gemini25Flash without breaking existing gpt55* rawValues
    - preset.claudeModelID and preset.geminiModelID return correct model strings for their cases and nil otherwise
  </behavior>
  <read_first>
    GooseSwift/CoachChatTypes.swift (CoachModelPreset enum lines 57-91, CoachChatMessage lines 24-55)
    GooseSwift/GooseAppModel.swift (lines 1-10 — `@MainActor @Observable final class` declaration pattern)
    .planning/phases/18-coach-multi-provider/18-CONTEXT.md (D-04 preset table, D-06 protocol shape, D-07 registry shape)
  </read_first>
  <action>
    In GooseSwift/CoachChatTypes.swift, extend the existing `enum CoachModelPreset: String, CaseIterable, Identifiable` by adding new cases AFTER the existing `gpt55Low/gpt55Medium/gpt55High` (preserving rawValues): `claudeOpus48`, `claudeSonnet46`, `claudeHaiku45`, `gemini25Pro`, `gemini25Flash`. Extend the `title` switch with `String(localized:)` titles per UI-SPEC Copywriting Contract: "Claude Opus 4.8", "Claude Sonnet 4.6", "Claude Haiku 4.5", "Gemini 2.5 Pro", "Gemini 2.5 Flash". Add `var claudeModelID: String?` returning "claude-opus-4-8" / "claude-sonnet-4-6" / "claude-haiku-4-5-20251001" for the three Claude cases (nil otherwise) and `var geminiModelID: String?` returning "gemini-2.5-pro" / "gemini-2.5-flash" for the two Gemini cases (nil otherwise). Keep `modelID` and `effort` exhaustive (the new cases are not ChatGPT — give `effort` a `default` returning "medium"). Do NOT remove or renumber existing cases (D-04).

    Create GooseSwift/CoachProviderProtocol.swift with `import Foundation`. Define `protocol CoachProvider: AnyObject` exactly per D-06: `var id: String { get }`, `var displayName: String { get }`, `var isAuthenticated: Bool { get }`, `var availablePresets: [CoachModelPreset] { get }`, `func send(messages: [CoachChatMessage], systemPrompt: String, preset: CoachModelPreset) async throws -> AsyncStream<String>`, `func signOut()`.

    In the same file define `@MainActor @Observable final class CoachProviderRegistry` (pattern from GooseAppModel.swift lines 6-7). Properties: `private(set) var allProviders: [any CoachProvider]`, `var activeProvider: (any CoachProvider)?` whose `didSet` writes `activeProvider?.id` to UserDefaults key `private static let activeProviderDefaultsKey = "goose.coach.activeProviderId"`. In `init()`, build `allProviders` containing ONLY `ChatGPTCoachProvider()` this wave, with comment `// Wave 2-4: append ClaudeCoachProvider/CustomEndpointCoachProvider/GeminiCoachProvider here`. Resolve `activeProvider`: read the stored id from UserDefaults and match against allProviders; if none stored, default to the first authenticated provider, else the ChatGPT provider. Provide `func selectProvider(id: String)` that sets `activeProvider` to the matching instance from allProviders.
  </action>
  <verify>
    <automated>xcodebuild test -scheme GooseSwift -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:GooseSwiftTests/CoachProviderRegistryTests 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `GooseSwift/CoachProviderProtocol.swift` contains `protocol CoachProvider: AnyObject` with the exact six members from D-06
    - `CoachProviderRegistry` is declared `@MainActor @Observable final class` and uses UserDefaults key `"goose.coach.activeProviderId"`
    - `grep -c "case gpt55Low\|case gpt55Medium\|case gpt55High" GooseSwift/CoachChatTypes.swift` returns 3 (existing cases preserved)
    - `preset.claudeModelID` returns "claude-opus-4-8" for `.claudeOpus48` and nil for `.gpt55Low`
    - CoachProviderRegistryTests persistence test passes
  </acceptance_criteria>
  <done>Protocol + registry compile; CoachModelPreset extended without breaking existing cases; registry persistence test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Refactor OpenAICoachChatModel → CoachChatModel + ChatGPTCoachProvider + rewire call sites</name>
  <files>GooseSwift/CoachChatModel.swift, GooseSwift/OpenAICoachChat.swift, GooseSwift/ChatGPTCoachProvider.swift, GooseSwift/CoachView.swift, GooseSwift/CoachChatScreen.swift</files>
  <behavior>
    - CoachChatModel is @Observable @MainActor (no @Published, no ObservableObject)
    - ChatGPTCoachProvider.send() streams ChatGPT response text deltas through an AsyncStream<String>
    - CoachView builds CoachChatModel with @State (not @StateObject) and continues to stream ChatGPT replies
    - Existing conversation persistence, cancellation, and error handling behaviour is preserved
  </behavior>
  <read_first>
    GooseSwift/OpenAICoachChat.swift (full file — refactor target; send() lines 133-186, streamResponseLoop lines 188-261, handle lines 263-333, tool execution lines 376-402, cancellation/error helpers lines 605-621)
    GooseSwift/OpenAICoachResponsesClient.swift (OpenAIResponsesClient.stream signature line 156, SSE pattern, OpenAICoachRequestFactory lines 44-150)
    GooseSwift/CoachView.swift (lines 1-12 chat declaration, 27-51 + 73-98 chat wiring, 631-667 CoachProfileMenu)
    GooseSwift/CoachChatScreen.swift (line 4 @ObservedObject)
    .planning/phases/18-coach-multi-provider/18-PATTERNS.md (CoachChatModel migration section + ChatGPTCoachProvider wrapping)
  </read_first>
  <action>
    Create GooseSwift/ChatGPTCoachProvider.swift conforming to `CoachProvider`. `id = "chatgpt"`, `displayName = "ChatGPT"`, `availablePresets = [.gpt55Low, .gpt55Medium, .gpt55High]`. It owns `private let authClient = CodexSelfContainedAuthClient()` and `private let client = OpenAIResponsesClient()` (moved from OpenAICoachChatModel). `isAuthenticated` reflects whether a stored ChatGPT auth exists. Implement `send(messages:systemPrompt:preset:)` by wrapping the existing two-loop tool-call streaming logic (from OpenAICoachChat.swift `streamResponseLoop` + `handle`) inside `AsyncStream<String> { continuation in Task { … } }`, yielding `response.output_text.delta` text via `continuation.yield(delta)` and calling `continuation.finish()` at the end. Tool calls stay internal to ChatGPTCoachProvider (Research Pitfall 4). Move the tool-call helpers (`toolCall`, `fallbackToolID`, `completeToolCall`, `execute`, `summarizeToolOutput`, `jsonString`) into this provider.

    ChatGPT tool execution needs live `healthStore`/`appModel` (not the serialised systemPrompt). Resolve by adding `func bindToolContext(_ provider: @escaping () -> [String: Any])` on `ChatGPTCoachProvider` that `CoachChatModel` calls before `send()`; the provider's internal `execute(call:)` reads from the bound closure result. Document the chosen mechanism in the SUMMARY. Implement `signOut()` calling `authClient.clearStoredAuth()`.

    Rename GooseSwift/OpenAICoachChat.swift content into GooseSwift/CoachChatModel.swift: declare `@MainActor @Observable final class CoachChatModel`. Remove `ObservableObject` conformance and all `@Published` annotations, keeping `private(set)` (PATTERNS.md). Inject `CoachProviderRegistry` via `init(registry:)`. Route `send()` through `registry.activeProvider?.send(messages:systemPrompt:preset:)`, appending yielded deltas to the streaming assistant message (reuse `appendAssistantText`/`finishAssistantMessage`/`markAssistantMessageCancelled`). Build `systemPrompt` by serialising `CoachLocalToolContext.build(healthStore:appModel:)` as JSON for non-ChatGPT providers; for ChatGPT, call `bindToolContext` with a closure returning the same context (Pitfall 4). Keep `contextualPrompt(for:)`, conversation persistence, `cancelStreaming`, and the cancellation/error classification helpers (lines 605-621). Move ChatGPT auth members (`deviceCode`, `loginStatus`, `startOAuthSignIn`, `refreshAuth`) into ChatGPTCoachProvider OR keep thin pass-throughs on CoachChatModel that delegate to the ChatGPT provider — expose `var isSignedIn: Bool { registry.activeProvider?.isAuthenticated ?? false }`. Empty/delete OpenAICoachChat.swift so the build references no `OpenAICoachChatModel`.

    Rewire call sites:
    - CoachView.swift line 7: replace `@StateObject private var chat = OpenAICoachChatModel()` with `@State private var registry = CoachProviderRegistry()` plus `@State private var chat: CoachChatModel` initialised from the registry (use the @Observable @State init pattern from Phase 17 GooseSwiftApp — assign in an explicit `init` or via `@State` with a custom initializer). Update all `chat.*` usages.
    - CoachChatScreen.swift line 4: `@ObservedObject var chat: OpenAICoachChatModel` → `var chat: CoachChatModel` (use `@Bindable var chat` only if a `$` binding into chat is needed).
    - CoachView.swift line 632 `CoachProfileMenu`: `@ObservedObject var chat: OpenAICoachChatModel` → `var chat: CoachChatModel`. CoachProfileMenu remains this wave; the settings sheet replaces it in Wave 5.
  </action>
  <verify>
    <automated>xcodebuild build -scheme GooseSwift -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | grep -v '^#' | grep -c 'error:' | { read n; [ "$n" -eq 0 ] && echo "BUILD OK" || echo "BUILD ERRORS: $n"; }</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "OpenAICoachChatModel" GooseSwift/` returns zero matches (class fully renamed)
    - `grep -n "@StateObject\|@ObservedObject" GooseSwift/CoachView.swift GooseSwift/CoachChatScreen.swift` returns zero matches for the chat property
    - `GooseSwift/CoachChatModel.swift` declares `@MainActor @Observable final class CoachChatModel` with no `@Published`
    - `GooseSwift/ChatGPTCoachProvider.swift` conforms to `CoachProvider` and its `send()` returns `AsyncStream<String>`
    - `xcodebuild build` reports zero `error:` lines
  </acceptance_criteria>
  <done>App builds; ChatGPT streaming routes through the registry's ChatGPTCoachProvider; no OpenAICoachChatModel references remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → ChatGPT backend | OAuth token crosses to chatgpt.com over TLS |
| app → Keychain | ChatGPT OAuth token read at startup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Information Disclosure | ChatGPT OAuth token in Keychain | mitigate | Token remains in existing Keychain via CodexSelfContainedAuthClient with `kSecAttrAccessibleAfterFirstUnlockThisDeviceOnly`; ChatGPTCoachProvider does not copy it to UserDefaults |
| T-18-02 | Tampering | TLS to ChatGPT backend | accept | URLSession enforces ATS; standard TLS sufficient for OAuth bearer auth (no pinning required for this single-user app) |
| T-18-03 | Information Disclosure | Error messages from stream failures | mitigate | `describe(_:)` helper surfaces LocalizedError descriptions only; raw tokens never interpolated into error strings |
| T-18-SC | Tampering | npm/pip/cargo installs | mitigate | Phase installs zero external packages (D-08); no install tasks |
</threat_model>

<verification>
- `xcodebuild build -scheme GooseSwift` → zero errors
- `grep -rn "OpenAICoachChatModel" GooseSwift/` → zero matches
- `xcodebuild test -only-testing:GooseSwiftTests/CoachProviderRegistryTests` → green
- Manual: open Coach tab, sign in with ChatGPT (or with existing token), send a message → reply streams as before
</verification>

<success_criteria>
1. CoachProvider protocol exists with exact D-06 signature
2. CoachProviderRegistry is @Observable, persists active provider id to "goose.coach.activeProviderId"
3. CoachChatModel is @Observable @MainActor and delegates to registry.activeProvider
4. ChatGPTCoachProvider wraps existing ChatGPT logic; existing token works with no user action (COACH-06)
5. Five Wave 0 test files compile under the test target
6. App builds with zero errors and zero OpenAICoachChatModel references
</success_criteria>

<artifacts>
## Artifacts this phase produces (Wave 1)

| Symbol | File | Kind |
|--------|------|------|
| `CoachProvider` | GooseSwift/CoachProviderProtocol.swift | protocol |
| `CoachProviderRegistry` | GooseSwift/CoachProviderProtocol.swift | @Observable class |
| `CoachChatModel` | GooseSwift/CoachChatModel.swift | @Observable class (renamed) |
| `ChatGPTCoachProvider` | GooseSwift/ChatGPTCoachProvider.swift | CoachProvider conformance |
| `CoachModelPreset.claudeModelID` / `.geminiModelID` + 5 new cases | GooseSwift/CoachChatTypes.swift | computed property + enum cases |
| `CoachProviderTests`, `CoachProviderRegistryTests`, `CoachKeychainTests`, `ClaudeProviderTests`, `CustomEndpointProviderTests` | GooseSwiftTests/ | XCTest classes (Wave 0 stubs) |
</artifacts>

<output>
Create `.planning/phases/18-coach-multi-provider/18-01-SUMMARY.md` when done
</output>
