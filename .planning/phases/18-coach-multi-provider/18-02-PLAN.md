---
phase: 18-coach-multi-provider
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - GooseSwift/ClaudeCoachProvider.swift
  - GooseSwiftTests/ClaudeProviderTests.swift
  - GooseSwiftTests/CoachKeychainTests.swift
autonomous: true
requirements: [COACH-02, COACH-03]
user_setup:
  - service: anthropic
    why: "Claude provider requires an Anthropic API key entered by the user"
    env_vars: []
    dashboard_config:
      - task: "Create an API key"
        location: "console.anthropic.com -> Settings -> API Keys"

must_haves:
  truths:
    - "ClaudeCoachProvider conforms to CoachProvider and streams Anthropic Messages API responses via SSE"
    - "Anthropic API key is stored in Keychain (service com.goose.swift.claude, account api-key) and never in UserDefaults"
    - "Claude SSE content_block_delta text_delta events are extracted into the AsyncStream<String>"
    - "isAuthenticated is true only when a key exists in Keychain"
  artifacts:
    - path: "GooseSwift/ClaudeCoachProvider.swift"
      provides: "ClaudeCoachProvider + inline ClaudeKeychain helper"
      contains: "ClaudeCoachProvider"
      min_lines: 120
  key_links:
    - from: "GooseSwift/ClaudeCoachProvider.swift"
      to: "https://api.anthropic.com/v1/messages"
      via: "URLSession.shared.bytes(for:) SSE streaming"
      pattern: "api\\.anthropic\\.com/v1/messages"
    - from: "GooseSwift/ClaudeCoachProvider.swift"
      to: "Keychain (com.goose.swift.claude)"
      via: "SecItemAdd/SecItemCopyMatching"
      pattern: "com\\.goose\\.swift\\.claude"
---

<objective>
Implement `ClaudeCoachProvider` (COACH-03) conforming to the `CoachProvider` protocol from Wave 1: stream from the Anthropic Messages API (`POST https://api.anthropic.com/v1/messages`) via SSE, and store the API key in Keychain (COACH-02, D-02). Replace the Wave 0 skip stubs in `ClaudeProviderTests.swift` and `CoachKeychainTests.swift` with real assertions.

Purpose: Add the first non-ChatGPT provider, proving the protocol abstraction works for an API-key-based backend with a different SSE event shape than OpenAI.
Output: `ClaudeCoachProvider.swift` (with inline `ClaudeKeychain`), real Claude unit tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-coach-multi-provider/18-CONTEXT.md
@.planning/phases/18-coach-multi-provider/18-RESEARCH.md
@.planning/phases/18-coach-multi-provider/18-PATTERNS.md
@.planning/phases/18-coach-multi-provider/18-01-SUMMARY.md

@GooseSwift/CoachProviderProtocol.swift
@GooseSwift/OpenAICoachResponsesClient.swift
@GooseSwift/RemoteServerPersistence.swift
@GooseSwift/CoachChatTypes.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ClaudeKeychain helper + Keychain roundtrip tests</name>
  <files>GooseSwift/ClaudeCoachProvider.swift, GooseSwiftTests/CoachKeychainTests.swift</files>
  <behavior>
    - ClaudeKeychain.save(key) then load() returns the same key
    - After delete(), load() returns nil
    - Saved items use kSecAttrAccessibleAfterFirstUnlockThisDeviceOnly
  </behavior>
  <read_first>
    GooseSwift/RemoteServerPersistence.swift (lines 57-109 — minimal API key Keychain pattern: baseQuery, save, load, delete)
    GooseSwift/CodexEmbeddedAuth.swift (lines 373-439 — CodexSelfContainedAuthKeychain co-location pattern)
    .planning/phases/18-coach-multi-provider/18-CONTEXT.md (D-02 — service "com.goose.swift.claude", account "api-key")
    GooseSwiftTests/CoachKeychainTests.swift (existing Wave 0 skip stubs to replace)
  </read_first>
  <action>
    Create GooseSwift/ClaudeCoachProvider.swift with `import Foundation` and `import Security`. Define an inline `private enum ClaudeKeychain` following RemoteServerKeychain (RemoteServerPersistence.swift lines 57-109): `private static let service = "com.goose.swift.claude"`, `private static let account = "api-key"` (D-02; single account per provider per D-03 — one key per service, no multi-account list). Implement `static func save(_ key: String) throws` (SecItemDelete then SecItemAdd with `kSecAttrAccessible = kSecAttrAccessibleAfterFirstUnlockThisDeviceOnly`), `static func load() throws -> String?` (return nil on errSecItemNotFound), `static func delete() throws`, and `private static func baseQuery() -> [String: Any]` with kSecClass/kSecAttrService/kSecAttrAccount. Define `enum ClaudeKeychainError: Error { case saveFailed(OSStatus), deleteFailed(OSStatus) }`. To make the enum testable, expose a thin internal facade `enum ClaudeCredentialStore { static func save/load/delete }` (or mark ClaudeKeychain `internal`) so the test target can call it via `@testable import GooseSwift`.

    Replace CoachKeychainTests.swift `testClaudeKeychainRoundtrip()`: remove the XCTSkip, save a unique test key (e.g. "test-key-\(UUID())"), assert `load()` returns it, call `delete()`, assert `load()` returns nil. Clean up in `tearDown()` by calling delete().
  </action>
  <verify>
    <automated>xcodebuild test -scheme GooseSwift -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:GooseSwiftTests/CoachKeychainTests/testClaudeKeychainRoundtrip 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "com.goose.swift.claude" GooseSwift/ClaudeCoachProvider.swift` returns at least 1
    - `grep -c "kSecAttrAccessibleAfterFirstUnlockThisDeviceOnly" GooseSwift/ClaudeCoachProvider.swift` returns at least 1
    - CoachKeychainTests testClaudeKeychainRoundtrip passes (no XCTSkip)
  </acceptance_criteria>
  <done>Claude Keychain helper round-trips a key and the test passes without skip.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ClaudeCoachProvider streaming + SSE delta extraction</name>
  <files>GooseSwift/ClaudeCoachProvider.swift, GooseSwiftTests/ClaudeProviderTests.swift</files>
  <behavior>
    - extractClaudeDelta(from:) returns "Hello" for a content_block_delta/text_delta data line and nil for other event lines
    - send() builds a POST to https://api.anthropic.com/v1/messages with x-api-key and anthropic-version: 2023-06-01 headers
    - isAuthenticated is true only when ClaudeKeychain.load() returns a non-nil key
    - availablePresets returns [.claudeOpus48, .claudeSonnet46, .claudeHaiku45]
  </behavior>
  <read_first>
    GooseSwift/OpenAICoachResponsesClient.swift (lines 153-243 — URLRequest build, URLSession.shared.bytes SSE loop, JSONSerialization body)
    GooseSwift/CoachProviderProtocol.swift (CoachProvider protocol shape from Wave 1)
    .planning/phases/18-coach-multi-provider/18-RESEARCH.md (Pattern 2 Claude SSE parsing, Pattern 7 Claude request format, Code Examples Claude full request)
    GooseSwiftTests/ClaudeProviderTests.swift (Wave 0 skip stub to replace)
  </read_first>
  <action>
    In GooseSwift/ClaudeCoachProvider.swift, define `final class ClaudeCoachProvider: CoachProvider`. `id = "claude"`, `displayName = "Claude"`, `availablePresets = [.claudeOpus48, .claudeSonnet46, .claudeHaiku45]`. `var isAuthenticated: Bool { (try? ClaudeKeychain.load()) != nil }` (the inner `??` ensures the optional-optional collapses to a Bool). Implement `signOut()` calling `try? ClaudeKeychain.delete()`.

    Implement `send(messages:systemPrompt:preset:)` per RESEARCH Code Examples (Claude full request): build `URLRequest` to `https://api.anthropic.com/v1/messages`, POST, headers `x-api-key: {key}`, `anthropic-version: 2023-06-01`, `Content-Type: application/json`, `Accept: text/event-stream`, `timeoutInterval = 180`. Body via JSONSerialization: `model = preset.claudeModelID ?? "claude-sonnet-4-6"`, `max_tokens = 4096`, `system = systemPrompt`, `stream = true`, `messages = messages.map { ["role": $0.role == .user ? "user" : "assistant", "content": $0.text] }`. Wrap the SSE loop in `AsyncStream<String> { continuation in Task { … } }` following OpenAIResponsesClient.swift lines 183-204: `URLSession.shared.bytes(for:)`, guard 200..<300, `for try await line in bytes.lines { try Task.checkCancellation(); if let delta = extractClaudeDelta(from: line) { continuation.yield(delta) } }`, then `continuation.finish()`. On non-2xx or error, `continuation.finish()`.

    Add `func extractClaudeDelta(from line: String) -> String?` per RESEARCH Pattern 2: require `line.hasPrefix("data:")`, drop the prefix, JSONSerialization-parse, require `type == "content_block_delta"`, `delta.type == "text_delta"`, return `delta.text`. Make this method `internal` (not private) so the test can call it. If a missing key throws (no API key saved), throw a `ClaudeProviderError.missingAPIKey` before building the stream.

    Replace ClaudeProviderTests.swift `testClaudeDeltaExtraction()`: remove the XCTSkip, construct `ClaudeCoachProvider()`, assert `extractClaudeDelta(from:)` returns "Hello" for `data: {"type":"content_block_delta","index":0,"delta":{"type":"text_delta","text":"Hello"}}` and nil for `data: {"type":"message_start"}` and for `event: content_block_delta`. Add `testAvailablePresets()` asserting the three Claude presets.
  </action>
  <verify>
    <automated>xcodebuild test -scheme GooseSwift -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:GooseSwiftTests/ClaudeProviderTests 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "api.anthropic.com/v1/messages" GooseSwift/ClaudeCoachProvider.swift` returns at least 1
    - `grep -c "anthropic-version" GooseSwift/ClaudeCoachProvider.swift` returns at least 1
    - `ClaudeCoachProvider` conforms to `CoachProvider` and `send()` returns `AsyncStream<String>`
    - ClaudeProviderTests testClaudeDeltaExtraction passes asserting "Hello" extraction and nil for non-delta lines
  </acceptance_criteria>
  <done>ClaudeCoachProvider streams Anthropic responses; SSE delta extraction test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → api.anthropic.com | API key + chat content cross to Anthropic over TLS |
| app → Keychain | Anthropic API key stored and read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | Information Disclosure | Anthropic API key | mitigate | Stored only in Keychain (service com.goose.swift.claude) with `kSecAttrAccessibleAfterFirstUnlockThisDeviceOnly`; never written to UserDefaults or logs |
| T-18-05 | Tampering | TLS to api.anthropic.com | accept | URLSession ATS enforces https; standard TLS sufficient for API-key auth |
| T-18-06 | Information Disclosure | API key in error/log output | mitigate | extractClaudeDelta and send() never interpolate the key into error strings; ClaudeProviderError carries no key material |
| T-18-SC | Tampering | npm/pip/cargo installs | mitigate | Zero external packages (D-08); no install tasks |
</threat_model>

<verification>
- `xcodebuild test -only-testing:GooseSwiftTests/ClaudeProviderTests` → green
- `xcodebuild test -only-testing:GooseSwiftTests/CoachKeychainTests/testClaudeKeychainRoundtrip` → green
- `grep -rn "UserDefaults" GooseSwift/ClaudeCoachProvider.swift` → zero matches (key never in UserDefaults)
- Manual: enter Anthropic key in settings (Wave 5), send a message → reply streams
</verification>

<success_criteria>
1. ClaudeCoachProvider conforms to CoachProvider with the three Claude presets (COACH-03)
2. API key stored in Keychain service com.goose.swift.claude (COACH-02)
3. SSE content_block_delta/text_delta extraction works and is unit-tested
4. Keychain roundtrip test passes without skip
</success_criteria>

<artifacts>
## Artifacts this phase produces (Wave 2)

| Symbol | File | Kind |
|--------|------|------|
| `ClaudeCoachProvider` | GooseSwift/ClaudeCoachProvider.swift | CoachProvider conformance |
| `ClaudeKeychain` / `ClaudeCredentialStore` | GooseSwift/ClaudeCoachProvider.swift | Keychain helper enum |
| `ClaudeProviderError` | GooseSwift/ClaudeCoachProvider.swift | error enum |
| `extractClaudeDelta(from:)` | GooseSwift/ClaudeCoachProvider.swift | internal SSE parser |
</artifacts>

<output>
Create `.planning/phases/18-coach-multi-provider/18-02-SUMMARY.md` when done
</output>
